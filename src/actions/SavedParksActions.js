export const addToSavedParks = () => {
    return (dispatch) => {
        // fetch post request ?
        // fetch('http://localhost:3000/parks', )
        // .then()
        // dispatch({ type: 'ADD_TO_SAVED' , payload: parks})
    }
}